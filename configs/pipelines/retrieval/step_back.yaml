_target_: autorag_research.pipelines.retrieval.step_back.StepBackPipelineConfig
description: "Step-back prompting with abstract query generation and RRF fusion"
name: step_back
llm: mock
embedding: mock
rrf_k: 60
top_k: 10
batch_size: 128
max_concurrency: 16
max_retries: 3
retry_delay: 1.0
